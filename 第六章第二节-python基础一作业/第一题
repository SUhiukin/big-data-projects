a = int(input())  
#输入一个数字，但是发现从键盘进来是字符串，要转换成数字

if a % 2 == 0:  #判断被2整除
    if a % 3 == 0:#能被2整除的基础上，判断被3整除
        print("a都可以被2，3整除")
    else :
        print("a可以被2整除，不能被3整除")
elif a % 3 == 0: #不能被2整除的基础上，判断被3整除
    print("a可以被3整除，不能被2整除")
else:#两者都不行
        print("a都不可以被2，3整除")
